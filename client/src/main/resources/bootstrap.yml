eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka/
spring:
  cloud:
    config:
      name: config
      profile: dev
      label: master
      discovery:
        enabled: true
        service-id: spring-cloud-config-git-server